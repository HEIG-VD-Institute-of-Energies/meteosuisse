[pytest]
addopts = -ra --cov=meteosuisse --cov-report=term-missing
markers =
    unit: fast tests without network
    integration: recorded network tests
    live: hits real network (requires RUN_LIVE_TESTS=1)
    slow: long-running tests
filterwarnings =
    ignore::DeprecationWarning


